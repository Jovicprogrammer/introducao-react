

export function calculaMedia(n1, n2) {

    (n1 + n2) = soma
    mediaGeral = soma / 2

    return (`a media é ${mediaGeral}`)
}

export function mostrarNome(nome){
    console.log(`o nome é: ${nome}`)
}

// module.exports = {
//     calculaMedia
// }